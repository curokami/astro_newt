---
import { IconSearch, IconTags, IconInfoCircle } from "@tabler/icons-react";
---

<header class="header">
  <div class="container">
    <a href="/" class="logo">DevRun</a>
    <nav class="nav-menu">
      <a href="/tags">
        <IconTags size={24} stroke={2} /> Tags
      </a>
      <a href="/about">
        <IconInfoCircle size={24} stroke={2} /> About
      </a>
      <!-- ✅ `http://localhost:4321/blog` に遷移するように修正 -->
      <a href="/blog" class="search-icon">
        <IconSearch size={24} stroke={2} />
      </a>
    </nav>
    <button class="hamburger" onclick="toggleMenu()">☰</button>
    <!-- ✅ 「My Blog」を削除 -->
  </div>
</header>

<style>
  /* ✅ ヘッダーのスタイル */
  .header {
    background: white;
    padding: 15px 20px;
    box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
  }

  .header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1000px;
    margin: 0 auto;
  }

  /* ✅ DevRun のサイズを大きくする */
  .logo {
    font-size: 2.5rem; /* 🔥 ここを変更 */
    font-weight: bold;
    text-decoration: none;
    color: black;
  }

  .nav-menu {
    display: flex;
    gap: 20px;
  }

  .nav-menu a, .nav-menu button {
    text-decoration: none;
    color: black;
    font-size: 1rem;
    background: none;
    border: none;
    cursor: pointer;
    padding: 5px 10px;
  }

  .hamburger {
    display: none;
    font-size: 1.5rem;
    background: none;
    border: none;
    cursor: pointer;
  }

  @media (max-width: 768px) {
    .nav-menu {
      display: none;
      flex-direction: column;
      position: absolute;
      top: 60px;
      right: 20px;
      background: white;
      padding: 10px;
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
    }

    .nav-menu.active {
      display: flex;
    }

    .hamburger {
      display: block;
    }
  }
</style>